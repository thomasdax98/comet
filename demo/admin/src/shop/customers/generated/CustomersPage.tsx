// This file has been generated by comet admin-generator.
// You may choose to use this file as scaffold by moving this file out of generated folder and removing this comment.

import { Stack, StackPage, StackSwitch } from "@comet/admin";
import * as React from "react";
import { useIntl } from "react-intl";

import { CustomerForm } from "./CustomerForm";
import { CustomersGrid } from "./CustomersGrid";

export function CustomersPage(): React.ReactElement {
    const intl = useIntl();
    return (
        <Stack topLevelTitle={intl.formatMessage({ id: "customers.customers", defaultMessage: "Customers" })}>
            <StackSwitch>
                <StackPage name="grid">
                    <CustomersGrid />
                </StackPage>
                <StackPage name="edit" title={intl.formatMessage({ id: "customers.editCustomer", defaultMessage: "Edit Customer" })}>
                    {(selectedId) => <CustomerForm id={selectedId} />}
                </StackPage>
                <StackPage name="add" title={intl.formatMessage({ id: "customers.addCustomer", defaultMessage: "Add Customer" })}>
                    <CustomerForm />
                </StackPage>
            </StackSwitch>
        </Stack>
    );
}
